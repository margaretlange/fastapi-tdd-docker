sudo apt-get install -y postgresql-client

Do I have any notes on this already?

now how do i find the rds address. maybe I can use the aws cli.

aws rds describe-db-instances
under endpoint, address:
production.cke8al3jes86.us-west-2.rds.amazonaws.com


psql -h production.cke8al3jes86.us-west-2.rds.amazonaws.com -U postgres -d my_db
called my_db i guess
postgres://production.cke8al3jes86.us-west-2.rds.amazonaws.com/my_db 

make sure the DATABASE_URL is well formed. that was my issue
here is the correct format:
'postgres://[user]:[password]@production.cke8al3jes86.us-west-2.rds.amazonaws.com:5432/my_db'

something off with migrations

I need to figure out how to sync migrations
-- kill rds then restart
-- make sure problem is with migrations and not rds treatment of foreign keys

"aws_db_instance" "default"

terraform plan -replace="aws_db_instance.default"

terraform apply -replace="aws_db_instance.default"

i think i should just be able to run upgrade on a new database if the pyproject.toml and migrations files are there
try this out remotely
yes this works, just make sure both those folders are present

note that I did learn that just erasing the RDS tables didn't work well.

LOCAL POSTGRES
psql -h production.cke8al3jes86.us-west-2.rds.amazonaws.com -U postgres -d my_db

sudo -u postgres psql
DROP Database web_dev;
CREATE DATABASE web_dev;
GRANT ALL PRIVILEGES ON DATABASE web_dev TO maggie;
psql -h localhost -U maggie -d web_dev

postgres://maggie:[passwd]@host.docker.internal:5432/web_dev

select auth_sub from "user";
select * from "user";
You need quotes because user is a reserved key name.
https://stackoverflow.com/questions/22256124/cannot-create-a-database-table-named-user-in-postgresql
