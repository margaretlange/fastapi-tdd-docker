name: Continuous Integration and Delivery

on: [push]

env:
  API_IMAGE: ghcr.io/margaretlange/fastapi-tdd-docker/summarizer

jobs:
  build:
    name: Build API
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
      - name: Set up ssh agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.TESTING_API_SSH_PRIVATE }}
      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Pull image
        run: docker pull ${{ env.API_IMAGE }}:latest || true
      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          ssh: |
            default=${{ env.SSH_AUTH_SOCK }}
          cache-from: ${{ env.API_IMAGE }}:latest
          tags: ${{ env.API_IMAGE }}:latest
          file: ./services/summaries/Dockerfile.prod
          context: ./services/summaries
  test:
    name: Test Docker Image
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Pull image
        run: docker pull ${{ env.API_IMAGE }}:latest || true
      - name: Get Auth0 Token
        id: get_auth0_token
        run: |
          response=$(curl --request POST \
          --url https://dev-lhn1ufqgkgz8lk5w.us.auth0.com/oauth/token \
          --header 'content-type: application/json' \
          --data '{
            "client_id": "${{ secrets.AUTH0_CLIENT_ID }}",
            "client_secret": "${{ secrets.AUTH0_CLIENT_SECRET }}",
            "audience": "${{ secrets.AUTH0_AUDIENCE }}",
            "grant_type": "client_credentials"
          }')
          echo "::set-output name=access_token::$(echo $response | jq -r .access_token)"
      - name: Set Auth0 Token as Environment Variable
        run: echo "AUTH0_ACCESS_TOKEN=${{ steps.get_auth0_token.outputs.access_token }}" >> $GITHUB_ENV
      - name: Run container
        run: |
          docker run \
            -d \
            --name test-api \
            -e PORT=8765 \
            -e ENVIRONMENT=dev \
            -e OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }} \
            -e TAVILY_API_URL=${{ secrets.TAVILY_API_KEY }} \
            -e AUTH0_AUDIENCE=${{ secrets.AUTH0_AUDIENCE }} \
            -e AUTH0_DOMAIN=${{ secrets.AUTH0_DOMAIN }} \
            -e JWT_TEST_TOKEN=$${{ env.AUTH0_ACCESS_TOKEN }} \
            -e DATABASE_URL=sqlite://sqlite.db \
            -e DATABASE_TEST_URL=sqlite://sqlite.db \
            -p 5003:8765 \
            ${{ env.API_IMAGE }}:latest
      - name: Pytest
        run: docker exec test-api python -m pytest .
      - name: Flake8
        run: docker exec test-api python -m flake8 .
      - name: Black
        run: docker exec test-api python -m black --check .
      - name: isort
        run: docker exec test-api python -m isort --check .
